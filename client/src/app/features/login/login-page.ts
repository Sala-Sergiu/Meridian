import { Component } from '@angular/core';

// Placeholder only — the real login screen lands in a later slice.
@Component({
  selector: 'app-login-page',
  template: `
    <section>
      <h2>Login</h2>
      <p>Login screen placeholder — built in a later slice.</p>
    </section>
  `,
})
export class LoginPage {}
