import { Component } from '@angular/core';

// Placeholder only — the real onboarding board lands in a later slice.
@Component({
  selector: 'app-board-page',
  template: `
    <section>
      <h2>Board</h2>
      <p>Onboarding board placeholder — built in a later slice.</p>
    </section>
  `,
})
export class BoardPage {}
