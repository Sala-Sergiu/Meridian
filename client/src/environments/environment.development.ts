// Development values — ng serve swaps this file in via angular.json.
// Points at the .NET API's dev HTTP endpoint (launchSettings.json).
export const environment = {
  apiBaseUrl: 'http://localhost:5044/api',
};
